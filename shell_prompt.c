#include "shell.h"

/**
 * display_prompt - Function that displays the customised prompt
 * on the terminal
 * custom_printf - Customised printf function
 *
 * Return: 0
 */

void display_prompt(void)
{
	custom_printf("kola && hammed_shell# ");
}
